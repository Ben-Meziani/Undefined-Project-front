
const initialState = {
  email: 'toto@tata.com',
  password: '123',
};

const login = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default login;
